[project]
name = "skinstinct-content-bot"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = [
  "google-genai>=1.47",
  "requests",
  "python-dotenv",
  "tzdata",
]

[tool.vercel]
entrypoint = "app:app"
